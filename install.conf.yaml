- defaults:
    link:
      relink: true
      create: true

- clean: ['~']

- link:
    ~/.alias.sh:
    ~/.alacritty.yml:
    ~/.env_var.sh:
    ~/.brew_install.sh:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
